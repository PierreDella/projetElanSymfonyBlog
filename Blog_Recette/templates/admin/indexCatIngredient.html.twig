
{% extends 'admin.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
<table>
    <thead>
        <tr>
            <th>Nom</th>
            <th></th>            
        </tr>
    </thead>

    <tbody>
        {% for categorie in categories %}
            <tr>
                <td>
                    {{ categorie.name }}
                </td>
                <td>
                    <a href="{{ path('edit_catIngredient', {'id':categorie.id}) }}">Edit</a>
                </td>
            </tr>
        {% endfor %}    
    </tbody>
</table> 
<br>
                <a class= "icon_add" href="{{ path('new_catIngredient') }}"><button type="button" class="btn btn-dark">Ajouter</button></a>
            <br><br>  
{% endblock %}