<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<meta name="viewport">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		{% block stylesheets %}
			<link>
		{% endblock %}
	</head>
	<body>
		<nav>
			<div>

				{% if app.user %}
                    <ul>
                        <li>
                            <a href='{{ path('user_show', {'id':app.user.id}) }}'>Profil</a>
                        </li>
                        <li>
                            <a href="{{ path('user_index') }}">Utilisateurs</a>
                        </li>
                         <li>
                            <a href="{{ path('app_logout') }}">Deconnexion</a>
                        </li>
                        <li>
                            <a href="{{ path('recipe_index') }}">Recettes</a>
                        </li>
                         <li>
                            <a href="{{ path('new_recipe') }}">Ajout Recette</a>
                        </li>
                         {# <li>
                        
                        </li> #}
                   
				{% endif %}
                        <li>
                            <a href="{{ path('home') }}">Home</a>
                        </li>
                        <li>
                            <a href="{{ path('app_register') }}">Inscription</a>
                        </li>
                        <li>
                            <a href="{{ path('app_login') }}">Connexion</a>
                        </li>
                     </ul>
			</div>
		</nav>
		{% block body %}{% endblock %}
		{% block contenu %}{% endblock %}
		{% block javascripts %}{% endblock %}
	</body>
</html>
